# LORA — runtime dependencies (production).
# Pinned to exact versions for reproducible deploys.
# For test/dev tooling, see requirements-dev.txt.
#
# To upgrade a package: `pip install --upgrade <package>` then `pip freeze | grep <package>`
# and update this file with the new pinned version.

# ----- Web framework -----
Django==5.2.14
djangorestframework==3.17.1
django-filter==25.2
django-environ==0.13.0
django-csp==4.0
django-auditlog==3.4.1

# ----- Database -----
psycopg2-binary==2.9.12

# ----- Background scheduling -----
# Scheduled jobs run from a Railway cron job: `python manage.py run_scheduled_jobs`.
# No in-process scheduler library — see apps/core/management/commands/run_scheduled_jobs.py.

# ----- AI / LLM -----
openai==2.40.0
pydantic==2.13.4                  # Output schema validation in apps/ai/schemas.py
phonenumbers==9.0.31              # International phone PII detection in apps/ai/tokenizer.py

# ----- File handling / Images / PDF -----
Pillow==12.2.0
weasyprint==68.1                  # PDF generation (requires GTK+ system libs on host)
python-magic==0.4.27              # MIME type sniffing for upload validation
filetype==1.2.0                   # File type detection (uploads)
bleach==6.3.0                     # HTML sanitization (XSS prevention)

# ----- External integrations -----
requests==2.34.2                  # HTTP client for Zendesk/PayPal APIs
playwright==1.60.0                # Browser automation for Zendesk screenshots
tenacity==9.1.4                   # Retry decorators for external calls

# ----- Security -----
cryptography==48.0.0              # Field-level encryption in apps/config/encrypted_fields.py

# ----- Observability -----
structlog==25.5.0                 # Structured logs with correlation IDs

# ----- Production server -----
gunicorn==26.0.0
whitenoise[brotli]==6.12.0        # Serve static files in production (no CDN needed)
